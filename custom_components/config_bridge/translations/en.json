{
  "issues": {
    "report": {
      "title": "Config bridge didn't apply {kind}",
      "description": "{reason}\n\nWhat the YAML asks for, that Home Assistant doesn't have:\n\n{changes}\n\nNothing was written for {kind}; the bridge's other kinds are unaffected. It tries again at every restart, and this issue clears once {kind} matches the YAML."
    }
  },
  "services": {
    "export": {
      "name": "Export",
      "description": "Returns what Home Assistant currently has for each kind the bridge manages, in the shape of the config_bridge YAML, with secrets redacted. Use it to write the YAML in the first place."
    }
  }
}
